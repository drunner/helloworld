#!/bin/bash

SERVICENAME="$1"
CONTAINERNAME="$2"

# This script is run inside the container, not on the host.
# Often there's nothing to do here.
# An exception could be if the container does something on a remote host 
# on install and needs to roll that back on destroy.

echo "drdestroy completed succesfully."
